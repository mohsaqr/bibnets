# Build a network where entities share values from another field

Build a network where entities share values from another field

## Usage

``` r
build_by_network(
  data,
  field,
  by,
  counting,
  similarity,
  threshold,
  min_occur,
  top_n = NULL,
  self_loops = FALSE,
  deduplicate = TRUE,
  strip_quotes = TRUE
)
```
