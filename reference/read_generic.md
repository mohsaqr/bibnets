# Read a generic CSV with user-specified columns

Read a generic CSV with user-specified columns

## Usage

``` r
read_generic(
  file,
  id = NULL,
  sep = ";",
  authors = NULL,
  keywords = NULL,
  references = NULL,
  countries = NULL,
  affiliations = NULL,
  journal = NULL,
  list_cols = NULL
)
```
