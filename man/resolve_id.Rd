% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{resolve_id}
\alias{resolve_id}
\title{Resolve the work-identifier column}
\usage{
resolve_id(data, id = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{id}{\code{NULL} or a single column name (character scalar).}
}
\value{
\code{data} with a guaranteed character \code{id} column.
}
\description{
Materializes a top-level \code{id} column that the network pipeline uses to
index works (matrix rows). Resolution rules:
}
\details{
\itemize{
\item \code{id = NULL} (default): use the existing \code{id} column if one is present,
otherwise fall back to row numbers (\code{seq_len(nrow(data))}).
\item \code{id = "colname"}: copy the named column to \code{id}. The column must exist.
}

When \code{id} names a column other than \code{"id"} and the data \emph{already} has a
distinct \code{"id"} column, the request is ambiguous (the existing \code{"id"}
column might itself be an entity field). Rather than silently overwriting
it, this errors and asks the caller to resolve the conflict.
}
\keyword{internal}
