% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse-names.R
\name{parse_names}
\alias{parse_names}
\title{Reorder and parse author names}
\usage{
parse_names(
  x,
  format = c("first_last", "last_initials", "last"),
  surname_first = c("auto", "yes", "no")
)
}
\arguments{
\item{x}{Character vector of author names, one name per element.
\code{NA} and empty strings are preserved.}

\item{format}{Output style for personal names (group/corporate
authors, \code{NA}, and empty strings are returned unchanged in every
style). One of:
\describe{
\item{\code{"first_last"}}{(default) \code{"Saqr, Mohammed"} ->
\code{"Mohammed Saqr"}.}
\item{\code{"last_initials"}}{\code{"Saqr, Mohammed"} -> \code{"Saqr M."};
multiple given names become concatenated initials
(\code{"Garcia Marquez G.J."}); any suffix is appended
(\code{"Smith J. Jr"}).}
\item{\code{"last"}}{surname only, including any particle
(\code{"van der Berg"}, \code{"de la Cruz"}).}
}}

\item{surname_first}{How to read \strong{comma-less} strings (strings with
a comma are always \code{"Last, First"}). One of:
\describe{
\item{\code{"auto"}}{(default) surname-first when the trailing token
looks like initials — an all-uppercase token of 1-3 letters,
the Scopus / bibnets signature (\code{"WANG Y"} -> \code{"Y Wang"}'s
components). Otherwise treated as \code{"First Last"}. This is the
"bibnets takes precedence" bias: native bibnets/Scopus labels
parse correctly with no extra arguments, while ordinary
mixed-case \code{"First Last"} input is never misread.}
\item{\code{"yes"}}{force surname-first (\code{"Wang Yong"} -> surname
\code{Wang}, given \code{Yong}).}
\item{\code{"no"}}{force given-first (\code{"First Last"}); comma-less input
is returned unchanged.}
}
May also be given as the logical \code{TRUE} / \code{FALSE}. Inherently
ambiguous input (e.g. uppercase \code{"MOHAMMED LI"}) follows the \code{auto}
bias toward the bibnets/Scopus convention; pass \code{"no"} to override.}
}
\value{
A character vector the same length as \code{x}, formatted per
\code{format}. The parsed components are attached as the attribute
\code{"parts"} (independent of \code{format}): a data frame with columns
\code{original}, \code{first}, \code{last}, \code{particle}, \code{suffix}, and \code{type} (one
of \code{"person"}, \code{"organization"}, \code{"empty"}, \code{"missing"}). Casing of
the input is preserved; periods are stripped from parsed initials.
}
\description{
Converts author names to \code{"First Last"} order and breaks each name
into its components. The parser is aware of nobiliary particles
(\code{van}, \code{von}, \code{de}, \code{del}, \code{da}, \code{der}, ...) and generational
suffixes (\code{Jr}, \code{Sr}, \code{II}, \code{III}, \code{IV}), and is case-insensitive so
it handles bibnets' uppercased entity labels.
}
\details{
Three name conventions are recognised:
\itemize{
\item \code{"Last, First"} (a comma) — always parsed as surname-then-given.
\item \code{"SURNAME Initials"} (no comma, e.g. \code{"WANG Y"},
\code{"AYALA-ROMERO JA"}) — the Scopus / bibnets author-label form.
\item \code{"First Last"} (no comma, e.g. \code{"Mohammed Saqr"}).
}
Comma-less strings that look like group or corporate authors
(e.g. \code{"WHO Collaborating Group"}) are detected and left untouched, as
are \code{NA} and empty strings.

This is an optional, standalone utility. No reader or network builder
in \code{bibnets} calls it; entity labels are matched verbatim unless you
choose to apply this function yourself first.
}
\section{Input shape}{
\code{parse_names()} takes a \strong{flat character vector} (one name per
element) — not a data frame and not a list. bibnets readers store
authors as a \strong{list-column} (each element is a character vector,
because a paper has a variable number of authors), so map the
function over it rather than passing the column directly:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{df$authors <- lapply(df$authors, parse_names, format = "last_initials")
}\if{html}{\out{</div>}}

For an ordinary flat character column (or the \code{from} / \code{to} columns of
a \code{bibnets_network}), call it directly: \code{parse_names(df$col)}.
}

\section{Recommended workflow}{
Normalise names \strong{before} building a network, on the reader's
\code{authors} list-column. Node identity in bibnets is fixed when the
bipartite matrix is built (labels are upper-cased and matched
verbatim), so two spellings of one author (\code{"Saqr, Mohammed"} and
\code{"SAQR M"}) only merge into a single node if they are normalised
\emph{before} \code{author_network()} is called:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{d <- read_biblio("scopus.csv")
d$authors <- lapply(d$authors, parse_names, format = "last_initials")
net <- author_network(d, type = "collaboration")
}\if{html}{\out{</div>}}
}

\section{Applying to an existing edgelist}{
You \emph{can} call \code{parse_names()} on the \code{from} / \code{to} (or
\code{source} / \code{target}) columns of a built network, but it is a
per-column, graph-blind relabelling: edges, pairing, \code{weight} and
\code{count} are preserved, \strong{but}
\itemize{
\item apply the \emph{same call to both} endpoint columns or the two
ends use different labels;
\item the mapping is many-to-one, so distinct authors can collapse
onto one label (especially with \code{"last_initials"}), and bibnets
does \strong{not} re-aggregate the resulting duplicate edges.
}
Prefer the pre-build workflow above.
}

\section{Limitations}{
Comma-less names are inherently ambiguous; the \code{auto} heuristic is
biased toward the bibnets/Scopus surname-first convention and may
misread uppercase \code{"GIVEN SURNAME"} where the surname is 1-3 letters
(e.g. \code{"MOHAMMED LI"}). Suffix-first garbage
(\code{"Jr., Sammy Davis"}) is not specially handled. Use \code{surname_first}
to force interpretation when you know the source convention.
}

\examples{
parse_names(c("Saqr, Mohammed", "Lopez-Pernas, Sonsoles"))

# Alternative output styles
parse_names("Saqr, Mohammed", format = "last_initials")  # "Saqr M."
parse_names("Saqr, Mohammed", format = "last")            # "Saqr"
parse_names("van der Berg, Jan", format = "last_initials") # "van der Berg J."

x <- parse_names("Saqr, M.")
x
attr(x, "parts")

# Particles and suffixes
parse_names(c("van der Berg, Jan", "Smith, John, Jr.", "de la Cruz, Ana"))

# Scopus / bibnets surname-first labels are detected automatically
parse_names(c("WANG Y", "AYALA-ROMERO JA", "VAN DER BERG J"))
parse_names("WANG Y", format = "last_initials")          # "WANG Y."

# Override the auto heuristic when you know the convention
parse_names("Wang Yong", surname_first = "yes")          # "Yong Wang"

# Group authors are detected and left unchanged
parse_names("WHO Collaborating Group")

# Recommended workflow: normalise the authors list-column, then build
papers <- data.frame(id = c("P1", "P2", "P3"), stringsAsFactors = FALSE)
papers$authors <- list(
  c("Saqr, Mohammed", "Lopez, Ana"),
  c("SAQR M",         "Lopez, Ana"),
  c("Saqr, Mohammed", "Chen, Wei"))
papers$authors <- lapply(papers$authors, parse_names,
                         format = "last_initials")
net <- author_network(papers, type = "collaboration")
net
}
\seealso{
\code{\link[=author_network]{author_network()}} and \code{\link[=read_biblio]{read_biblio()}} for the upstream
stage where normalisation is best applied.
}
