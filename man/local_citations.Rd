% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historiograph.R
\name{local_citations}
\alias{local_citations}
\title{Compute local citation scores}
\usage{
local_citations(
  data,
  references = "references",
  sep = ";",
  strip_quotes = TRUE,
  id = NULL
)
}
\arguments{
\item{data}{A data frame with \code{id} and a references column (list-column
or delimited string). Optionally \code{year}, \code{title}, \code{journal}, \code{doi},
\code{cited_by_count}.}

\item{references}{Character. Name of the column containing cited references.
Default \code{"references"}.}

\item{sep}{Character. Separator used to split the references column when it is a plain
character column. Default \code{";"}.}

\item{strip_quotes}{Logical. If \code{TRUE} (default), surrounding quote
characters are removed from each reference.}

\item{id}{Optional. Name of the column to use as the work identifier.
If \code{NULL} (default), an existing \code{id} column is used when present,
otherwise row numbers are used.}
}
\value{
A data frame with columns:
\describe{
\item{\code{id}}{Document identifier.}
\item{\code{lcs}}{Local Citation Score: times cited within the dataset.}
\item{\code{gcs}}{Global Citation Score: \code{cited_by_count} if available.}
}
Plus any metadata columns present in the input (\code{title}, \code{year},
\code{journal}, \code{doi}).
}
\description{
Counts how many times each document is cited by other documents
within the dataset.
}
\examples{
data(biblio_data)
local_citations(biblio_data)
}
