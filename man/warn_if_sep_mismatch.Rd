% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/co-network.R
\name{warn_if_sep_mismatch}
\alias{warn_if_sep_mismatch}
\title{Warn when a separator likely failed to split a multi-entity column}
\usage{
warn_if_sep_mismatch(col, parts, field, sep)
}
\description{
Splitting with the wrong separator silently yields one "entity" per row
(e.g., a whole author byline as a single node). Heuristic: no row split
into more than one entity, yet most non-empty strings contain a common
\emph{structural} delimiter.
}
\details{
Only structural delimiters (\code{";"}, \code{"|"}, tab) are considered, because
they essentially never occur inside a single legitimate label. Commas
and \code{" and "} are deliberately excluded: they appear inside valid
single values (e.g. \code{"Last, First"} author names, one-reference-per-row
citation strings, or organisations like \code{"Smith and Sons"}), so warning
on them would mislead users with correct data.
}
\keyword{internal}
